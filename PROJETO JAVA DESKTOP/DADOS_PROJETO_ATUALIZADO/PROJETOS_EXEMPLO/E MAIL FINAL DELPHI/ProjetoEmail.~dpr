program ProjetoEmail;

uses
  Forms,
  email in 'email.pas' {fSendMail};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TfSendMail, fSendMail);
  Application.Run;
end.
